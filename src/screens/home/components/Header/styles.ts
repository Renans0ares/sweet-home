import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        padding: 16,
        paddingTop: 64,
        backgroundColor: "#4DA8DA",
    },
    text: {
        color: "#FFFFFF",
        fontSize: 20,
        textAlign: "center",
    },
});